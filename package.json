{
  "name": "github-daily-trends",
  "version": "1.0.0",
  "description": "GitHub Daily Trends - 日次トレンドリポジトリ解析サイト",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --port=13000",
    "clean": "rm -rf dist",
    "generate-data": "node scripts/generate-site-data.js",
    "build-all": "npm run generate-data && npm run build"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it": "^14.0.0",
    "markdown-it-anchor": "^8.6.7"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^11.1.1"
  }
}